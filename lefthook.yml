# See: https://lefthook.dev/configuration/

pre-push:
  jobs:
    - name: mise run check-full
      run: mise run check-full

pre-commit:
  jobs:
    - name: mise run check
      run: mise run check

    - name: secret check
      run: mise run secret-check
